#include <stdio.h>

/** main - function that prints the number
 *        of arguments passed into it.
 * @argv: ls
 * @argc: int
 * Return: 0
 */
int main(int argc, char  *argv[])
{
	printf("%d\n",argc - 1);
	return (0);
}
