#include <stdio.h>
#include <stdlib.h>

/**
 *
 */

int main(int argc, char const *argv[])
{
	if(argc == 1)
	{
		printf("0\n");
		return (0);
	}
	for (i = 0; i < argc; i++)
	{
		int n = 0;

		if(!isdigit(*n))
		{
			printf("Error\n");
			return (0);
		}
	return (0);
	}
